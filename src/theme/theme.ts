import { createTheme } from '@mui/material/styles';

const theme =  createTheme({
	palette: {
		primary : {
			main: '#071e22' ,
		},

		secondary :{
			main: '#337357' ,

		},

		background : {
			default: '#fff', 
		}

	}
});

export default theme;